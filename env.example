# Slack App Token (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token-here

# Slack Bot Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Slack Signing Secret
SLACK_SIGNING_SECRET=your-signing-secret-here

# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-db-password
DB_NAME=spinbot

# Environment
NODE_ENV=development

# Authorized Users (comma-separated Slack User IDs who can view stats)
# Get User ID: Right-click on user in Slack -> View Profile -> More -> Copy Member ID
AUTHORIZED_STATS_USERS=U12345678,U87654321

# Excluded Status Emojis (comma-separated emojis)
# Users with these emojis in their status will be excluded from channel-level selection
# Examples: 🏖️ (vacation), 🤒 (sick), 👶 (parental leave), 🏫 (school/training)
EXCLUDED_STATUS_EMOJIS=:palm_tree:,:face_with_thermometer:,:kids:,:schule:

# Maximum Channel Size for channel-level selection (default: 20)
# Channels larger than this will require thread usage to avoid API rate limits
MAX_CHANNEL_SIZE=20
